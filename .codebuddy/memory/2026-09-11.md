# 2026-09-11

## 华测 COM7_20260910_114132.bin 与供应商 Allan 对比结论

- 供应商 5 张图确认为同一数据段。全量不裁剪复算的最低 ADEV 等效 B 与供应商逐位吻合：gyro_x 2.877 vs 2.8749 °/h、acc_x 7.42 vs 7.40 μg、acc_y 6.14 vs 6.13 μg。
- 供应商图纵轴标 “AVAR” 但实际是 Allan 偏差 ADEV（白噪声区斜率 -1/2）。图中 N/B/K/R 虚线为 IEEE 952 风格噪声分项。
- **供应商 N 高估结论**：其 N 来自 tau≈0.02 s 附近的数字滤波滚降驼峰，不是白噪声平台区；本数据上会高估约 30~40%。主程序按 0.1~10 s 白噪声区外推 tau=1 s 的口径符合 IEEE 952/1780 惯例，保留该口径。
- 主程序 ADEV 核心算法无错；差异来源主要是：供应商全量不裁剪 vs 主程序掐头去尾 1 min、供应商模型拟合读数 vs 主程序白噪声平台读数、BI 平台判据严格性不同。
- 轴映射（按 B 值匹配）：102738=gyro_x，102758=gyro_z，102802=acc_x，102806=acc_y，102810=acc_z。

## 新增独立程序 IMU_AllanNoiseFit_huace.py

- 用户要求：不改主程序，生成带 N/B/K/R 虚线拟合的 09 号图，并融入原报告。
- 原代码已备份：`IMU_Analysis_huace_原始备份_20260911.py`，备份前后 SHA256 均为 `4F0A0BC2A9FBCEEF7B0A1ADFEAABB668984FDF482C567C18D9D0D221C6962FAE`。
- 新程序复用主程序 `parse_binary_file`（只读导入），ADEV 使用 allantools 经典非重叠 ADEV、500 Hz、与主报告一致的 1 min 掐头去尾。
- 输出：`analysis_results/09_Allan噪声分项拟合图_NBKR.png`、`09_Allan噪声分项拟合参数.csv`、`09_Allan噪声分项拟合说明.md`。
- N 标准值已与主程序报告一致：acc N≈0.01050/0.01035/0.01142 m/s/√h；gyro N≈0.07328/0.09262/0.08074 °/√h。
- 图中单位体系：陀螺纵轴 °/h，N 绘图系数 = 标准值×60；加计纵轴 μg，N 绘图系数 = 标准值/(G0×60e-6)。标题与 CSV 输出标准单位，绘图内部用 N_draw。
- K/R 在本 25 min 记录上未识别出独立斜率区，图和说明均标 N/A/受记录长度限制，避免无依据拟合。
- MD/HTML 报告已追加第 9 节：`9. Allan噪声分项拟合图（N/B/K/R）`，指向 09 图并说明参数口径。


## 09号图并入主程序（后续修订）

- 应用户要求，09号 Allan 噪声分项拟合功能已并入 `IMU_Analysis_huace.py` 的 `plot_allan_noise_fit()`，作为独立方法，不改既有 Allan 参数提取逻辑。
- 删除对独立脚本 `IMU_AllanNoiseFit_huace.py` 的依赖；主程序一次 `run_full_analysis()` 即可生成 01~09 全部报告。
- 09号图风格对齐供应商图：品红圆点=实测ADEV；绿色线=拟合合成；灰色虚线=N/B/K/R 分项；左上角白色参数框；陀螺纵轴 °/h，加计纵轴 m/s²。
- N 仍按 0.1~10 s 白噪声平台区拟合并外推 τ=1 s（IEEE 952/1780 惯例），不拟合 τ<0.1 s 数字滤波滚降驼峰。
- 主程序编译通过，实际运行完整流水线成功生成 `09_Allan噪声分项拟合图.png`，报告 MD/HTML 已自动包含第 9 节。
- 原代码备份保留在 `IMU_Analysis_huace_原始备份_20260911.py`。

## 中断恢复与当前 09 图修订（2026-09-11）

- 本次中断位置已确认：主程序 `IMU_Analysis_huace.py` 的 `plot_allan_noise_fit()` 在绘制 K 分项时残留使用了已删除的 `th` 变量，触发 `NameError: name 'th' is not defined`；已改为使用秒制 `tgrid`，R 分项同样改为 `tgrid`。
- 中断后的单方法测试已通过：`COM7_20260910_114132.bin` 解析 762,863 点，500 Hz，头尾各裁剪 1 分钟后约 1525.726 s；09 图可以生成。
- 完整流水线已继续运行并通过 13/13 步骤，01～09 及 HTML/Markdown 报告已重新生成。主程序的 `report_data` 中包含 09 图路径；报告中 09 节图片链接已更新。
- 视觉检查后又修正 09 图：实测 ADEV 改为“品红色连续线+圆点”；拟合线绿色；N/B/K/R 使用不同颜色虚线并在右侧标注；x 轴固定 10^-3～10^3 s；纵轴范围包含实测、合成线和分项线；每个子图保持主程序一次运行生成。
- 重要单位修正：09 图内部统一在绘图单位和 tau 秒中计算；陀螺 °/h 下 `K` 显示换算为 `k_plot*60`，`R` 为 `r_plot*3600`。此前 `sqrt(3*3600)` 和 `sqrt(2)*3600` 会给出不一致的 K 单位显示，已修正。
- 当前 09 图仍是六轴 2×3 汇总图，不是供应商的单轴五张图；这是布局差异，不应声称已经“完全一样”。供应商使用全量记录且有 data1～data7、M1/M2/Q 等内部模型分项；当前主程序只基于已确认的 N/B/K/R 口径绘制，未凭空补造供应商未公开的内部分项。
- N 口径保持用户明确要求：0.1～10 s 白噪声区的 -1/2 斜率段外推到 tau=1 s；不把约 0.02 s 数字滤波滚降峰值当 ARW。
- 当前尚未进行 Git 提交或 GitHub 推送；推送前应先由用户确认当前六轴布局及 K/R 估计显示是否接受。

## 09号图推广到全部五个分析程序（2026-09-11 下午）

- 华测原始备份 `IMU_Analysis_huace_原始备份_20260911.py`（SHA256=4F0A0BC2…62FAE）已在用户确认“原始代码未改动”后删除；此前 diff 确认：01～08 算法与 BIN 解析零改动，仅新增 `plot_allan_noise_fit()` 及流水线/报告挂载点。
- 09号图功能以同名独立方法 `plot_allan_noise_fit()` 加入其余四个程序：`IMU_Analysis_huayi.py`、`IMU_Analysis_yuanshen.py`、`IMU_Analysis_linsi_LINS16460.py`、`IMU_Analysis_linsi_LINS355.py`；不改各设备解析结构与既有 Allan 参数提取逻辑。
- 元生脚本使用模块级 `build_allan_display_taus()` + 常量 `ALLAN_DISPLAY_POINTS_PER_DECADE`；其余三个用类静态方法 `self._build_allan_display_tau_grid()`，集成时已分别适配。
- 各程序 `run_full_analysis()` 在 Allan 偏差图之后新增步骤“绘制Allan噪声分项拟合图”；HTML/MD 报告的 hidden_report_keys 与 image_keys 均追加“09/Allan噪声分项拟合图”，编号按各程序既有图数顺延（元生/凌思为第8张，华依为第9张）。
- 验证：四个程序均 `py_compile` 通过，并用真实数据各跑一次 09 图生成成功——华依 3,699,235 行(1000Hz)、元生 1,868,973 行(200Hz)、LINS16460 720,117 行、LINS355 715,871 行；输出在各数据目录 `analysis_results/09_Allan噪声分项拟合图.png`。
- 口径保持：N=0.1~10s 白噪声平台区 -1/2 斜率拟合外推 τ=1s；绘图单位陀螺 °/h、加计 μg；K/R 为长τ残差模型估计并标注受记录长度限制。
